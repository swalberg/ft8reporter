<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:spots, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:spots, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:spots, :edit, :id => @spot.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :spot, url(:spots, :update, :id => @spot.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'spots/form', :locals => { :f => f } %>
  <% end %>
</div>
